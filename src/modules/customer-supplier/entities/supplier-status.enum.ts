/** Mirrors CustomerStatus — see customer-status.enum.ts for the ACTIVE/INACTIVE/BLOCKED rationale (Phase 11 §14). */
export enum SupplierStatus {
  Active = 'ACTIVE',
  Inactive = 'INACTIVE',
  Blocked = 'BLOCKED',
}
