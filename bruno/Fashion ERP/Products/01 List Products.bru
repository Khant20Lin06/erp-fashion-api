meta {
  name: 01 List Products
  type: http
  seq: 1
  tags: [
    products
    smoke
  ]
}

get {
  url: {{baseUrl}}/api/v1/products?companyId={{companyId}}
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("products list returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
