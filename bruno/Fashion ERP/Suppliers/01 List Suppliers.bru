meta {
  name: 01 List Suppliers
  type: http
  seq: 1
  tags: [
    suppliers
    smoke
  ]
}

get {
  url: {{baseUrl}}/api/v1/suppliers?companyId={{companyId}}
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("suppliers list returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
