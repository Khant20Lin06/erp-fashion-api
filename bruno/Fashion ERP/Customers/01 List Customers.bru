meta {
  name: 01 List Customers
  type: http
  seq: 1
  tags: [
    customers
    smoke
  ]
}

get {
  url: {{baseUrl}}/api/v1/customers?companyId={{companyId}}
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("customers list returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
