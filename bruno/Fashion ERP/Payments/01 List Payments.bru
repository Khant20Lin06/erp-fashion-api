meta {
  name: 01 List Payments
  type: http
  seq: 1
  tags: [
    payments
    smoke
  ]
}

get {
  url: {{baseUrl}}/api/v1/payments?companyId={{companyId}}
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("payments list returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
