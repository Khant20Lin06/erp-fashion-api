meta {
  name: 01 My Permissions
  type: http
  seq: 1
  tags: [
    rbac
    smoke
  ]
}

get {
  url: {{baseUrl}}/api/v1/auth/me/permissions
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("my permissions returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
