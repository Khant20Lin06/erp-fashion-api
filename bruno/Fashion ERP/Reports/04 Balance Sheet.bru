meta {
  name: 04 Balance Sheet
  type: http
  seq: 4
  tags: [
    reports
    accounting
  ]
}

get {
  url: {{baseUrl}}/api/v1/reports/balance-sheet?companyId={{companyId}}&branchId={{branchId}}&asOfDate={{asOfDate}}
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("balance sheet returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
