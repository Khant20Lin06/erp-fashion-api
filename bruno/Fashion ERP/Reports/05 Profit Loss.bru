meta {
  name: 05 Profit Loss
  type: http
  seq: 5
  tags: [
    reports
    accounting
  ]
}

get {
  url: {{baseUrl}}/api/v1/reports/profit-loss?companyId={{companyId}}&branchId={{branchId}}&fromDate={{fromDate}}&toDate={{toDate}}
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("profit and loss returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
