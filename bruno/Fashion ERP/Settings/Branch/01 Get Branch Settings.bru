meta {
  name: 01 Get Branch Settings
  type: http
  seq: 1
  tags: [
    settings
    branch
  ]
}

get {
  url: {{baseUrl}}/api/v1/settings/branch/{{branchId}}?companyId={{companyId}}
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}
