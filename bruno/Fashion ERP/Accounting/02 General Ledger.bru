meta {
  name: 02 General Ledger
  type: http
  seq: 2
  tags: [
    accounting
    smoke
  ]
}

get {
  url: {{baseUrl}}/api/v1/general-ledger?companyId={{companyId}}&branchId={{branchId}}&accountId={{accountId}}&fromDate={{fromDate}}&toDate={{toDate}}
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("general ledger returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
