meta {
  name: 01 Journal Entries
  type: http
  seq: 1
  tags: [
    accounting
    smoke
  ]
}

get {
  url: {{baseUrl}}/api/v1/journal-entries?companyId={{companyId}}
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("journal entries list returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
